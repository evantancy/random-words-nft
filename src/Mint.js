import React, { useState } from "react";
import { ethers } from "ethers";

const Mint = () => {
    return (
        <div className="Mint">
            <button>Mint</button>
        </div>
    );
};

export default Mint;
